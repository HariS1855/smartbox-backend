spring.application.name=SmartBox

# PostgreSQL connection (use environment variables if available)
spring.datasource.url=jdbc:postgresql://${PGHOST:postgres.railway.internal}:${PGPORT:5432}/${PGDATABASE:railway}
spring.datasource.username=${PGUSER:postgres}
spring.datasource.password=${PGPASSWORD:OlIxSFlUHUGMBznihmubQZfgThExIDyN}

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Flask API URL (use environment variable or default)
flask.api.url=${FLASK_API_URL:https://embryogenic-larissa-explicit.ngrok-free.dev/analyze}

# Server port (optional)
server.port=${PORT:8081}



